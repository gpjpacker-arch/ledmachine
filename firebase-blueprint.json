{
  "entities": {
    "site_settings": {
      "title": "Site Settings and CMS Content",
      "description": "Stores live website content, contacts, logos, texts and product data edited via Admin CMS",
      "type": "object",
      "properties": {
        "content": {
          "type": "object",
          "description": "Serialized SiteContent object"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of last update"
        },
        "updatedBy": {
          "type": "string",
          "description": "Admin identifier"
        }
      },
      "required": ["content"]
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /site_settings/{document=**} {\n      allow read: if true;\n      allow write: if true;\n    }\n    match /{document=**} {\n      allow read: if true;\n      allow write: if true;\n    }\n  }\n}"
}
